{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "command": "cd \"${CLAUDE_PROJECT_DIR}\" && git add -A && git diff --cached --quiet || (git commit -m \"auto: changes from Claude\" && git push)"
      }
    ]
  }
}
